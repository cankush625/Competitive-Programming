# Compress the characters given in the array in place.

The length after compression should be less than or equal to the original array

eg. Input: ['a', 'a', 'b', 'c', 'c', 'c']
    Output: ['a', '2', 'b', 'c', '3']